<windows:SettingsPageView
    x:Class="ImageGlass.Common.Windows.ViewerSettingsView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:types="using:ImageGlass.Common.Types"
    xmlns:ui="using:ImageGlass.UI"
    xmlns:windows="using:ImageGlass.Common.Windows"
    mc:Ignorable="d"
    d:DesignWidth="600"
    HorizontalAlignment="Stretch">

    <StackPanel Orientation="Vertical">

        <!--  Appearance  -->
        <ui:PhTextBlock Classes="group" LangKey="FrmSettings_Appearance" />
        <StackPanel Margin="{DynamicResource SettingsGroupIndent}">
            <CheckBox x:Name="PART_NavButtons">
                <ui:PhTextBlock LangKey="FrmSettings_EnableNavigationButtons" TextWrapping="Wrap" />
            </CheckBox>
            <CheckBox x:Name="PART_VectorRenderer">
                <ui:PhTextBlock LangKey="FrmSettings_EnableVectorRenderer" TextWrapping="Wrap" />
            </CheckBox>

            <!--  Checkerboard background mode  -->
            <StackPanel Margin="0,10,0,0" Spacing="{DynamicResource SettingsLabelInputGap}">
                <ui:PhTextBlock LangKey="FrmSettings_CheckerboardMode" />
                <ComboBox
                    x:Name="PART_CheckerboardMode"
                    Width="200"
                    HorizontalAlignment="Left" />
            </StackPanel>
        </StackPanel>


        <!--  Panning  -->
        <ui:PhTextBlock Classes="group" LangKey="FrmSettings_Panning" />
        <StackPanel Margin="{DynamicResource SettingsGroupIndent}" Spacing="10">
            <CheckBox x:Name="PART_FreePan">
                <ui:PhTextBlock LangKey="FrmSettings_EnableFreePan" TextWrapping="Wrap" />
            </CheckBox>

            <!--  Panning margin  -->
            <StackPanel Spacing="{DynamicResource SettingsLabelInputGap}">
                <ui:PhTextBlock LangKey="FrmSettings_PanMargin" />
                <ui:PhTextBox
                    x:Name="PART_PanMargin"
                    Width="200"
                    HorizontalAlignment="Left"
                    AcceptValue="UnsignedFloatValueOnly" />
            </StackPanel>

            <!--  Panning speed (0 - 100)  -->
            <StackPanel Spacing="{DynamicResource SettingsLabelInputGap}">
                <ui:PhTextBlock x:Name="PART_PanSpeedLabel" LangKey="FrmSettings_PanSpeed" />
                <Slider
                    x:Name="PART_PanSpeed"
                    Width="200"
                    HorizontalAlignment="Left"
                    IsSnapToTickEnabled="True"
                    Maximum="100"
                    Minimum="0"
                    TickFrequency="1" />
            </StackPanel>
        </StackPanel>


        <!--  Zooming  -->
        <ui:PhTextBlock Classes="group" LangKey="FrmSettings_Zooming" />
        <StackPanel Margin="{DynamicResource SettingsGroupIndent}" Spacing="10">

            <!--  Sub-group: Image interpolation  -->
            <ui:PhTextBlock Classes="subgroup" LangKey="FrmSettings_ImageInterpolation" />
            <StackPanel Margin="{DynamicResource SettingsGroupIndent}" Spacing="10">
                <StackPanel Spacing="{DynamicResource SettingsLabelInputGap}">
                    <ui:PhTextBlock LangKey="FrmSettings_ImageInterpolation_ScaleDown" />
                    <ComboBox
                        x:Name="PART_InterpolationScaleDown"
                        MinWidth="200"
                        HorizontalAlignment="Left" />
                </StackPanel>
                <StackPanel Spacing="{DynamicResource SettingsLabelInputGap}">
                    <ui:PhTextBlock LangKey="FrmSettings_ImageInterpolation_ScaleUp" />
                    <ComboBox
                        x:Name="PART_InterpolationScaleUp"
                        MinWidth="200"
                        HorizontalAlignment="Left" />
                </StackPanel>
            </StackPanel>

            <!--  Zoom speed (-500 - 500)  -->
            <StackPanel Spacing="{DynamicResource SettingsLabelInputGap}">
                <ui:PhTextBlock x:Name="PART_ZoomSpeedLabel" LangKey="FrmSettings_ZoomSpeed" />
                <Slider
                    x:Name="PART_ZoomSpeed"
                    Width="200"
                    HorizontalAlignment="Left"
                    IsSnapToTickEnabled="True"
                    Maximum="500"
                    Minimum="-500"
                    TickFrequency="1" />
            </StackPanel>

            <!--  Zoom levels  -->
            <StackPanel Spacing="{DynamicResource SettingsLabelInputGap}">
                <ui:PhTextBlock LangKey="FrmSettings_ZoomLevels" />
                <CheckBox x:Name="PART_UseSmoothZooming">
                    <ui:PhTextBlock LangKey="FrmSettings_UseSmoothZooming" TextWrapping="Wrap" />
                </CheckBox>
                <ui:PhTextBox
                    x:Name="PART_ZoomLevels"
                    Width="460"
                    MinHeight="70"
                    HorizontalAlignment="Left"
                    AcceptsReturn="True"
                    FontFamily="{x:Static types:Const.FONT_CODE}"
                    TextWrapping="Wrap" />
                <ui:PhButton
                    x:Name="PART_LoadDefaultZoomLevels"
                    HorizontalAlignment="Left"
                    Variant="Link" />
            </StackPanel>

        </StackPanel>

    </StackPanel>

</windows:SettingsPageView>
